# STS2 Guide 当前实现状态

> 更新时间：2026-07-05
>
> 产品依据：[`product-spec.md`](product-spec.md)
> 本文区分源码、自动测试、已安装版本和真机结果。

## 当前结论

P0 的“只读 Mod → 本地后台 → 游戏内选牌建议”已经完成一次真实闭环。稳定 Run ID、
真实地图结构和左侧抽屉面板均在本机游戏中出现过。

当前推荐器 v3、动态跳过、效果标签 v3、推荐索引高亮和更大字号均已进入本机安装
与运行态：

- Python 自动测试：122/122 通过；
- Mod：编译 0 error，工作区与游戏 `mods` 三个文件哈希一致；
- `realtime.host` 已验证能够加载 v3 源码，现已按开发阶段策略主动关闭；
- 真实数据库为 schema v7、effect tag v3，共 1657 条标签；
- 当前局检查点保留，Run ID 和 sequence 未因后台启停重置。

推荐器 v3 仍需下一次真实选牌验证，不能只凭离线回放标为最终完成。

## 已真机确认

- Mod 输出 schema v3 事件；
- Run ID `sts2-b326c84613b96618d9fa1099` 在同一局中保持稳定；
- sequence 能从检查点恢复并递增；
- 真实三张候选稳定 ID、牌组、HP、遗物和药水能够进入后台；
- 地图检查点曾读取 59 个节点、真实 `Children` 连边、当前位置和下一节点；
- 选牌建议能回到游戏内面板，选择界面关闭后面板销毁；
- 面板已移动为贴住左边缘的半透明抽屉，自适应宽度并可展开/收起；
- 实时路径没有向旧逐事件历史表追加记录。

尚未完成两张不同地图和 Boss 节点的系统回归。

## 工作区已实现并自动验证

### 当前局和数据生命周期

- `active-run.json` 是唯一可替换检查点，保存退出后可恢复；
- `events/` 只作为临时队列，处理后删除；
- 结束事件只向 `run_summaries` 写最终摘要，并清理当前局检查点；
- 旧 `run_states`、`decision_events`、`decision_outcomes`、
  `game_state_events` 不属于 P0 实时写入路径；
- 重启恢复、重复事件、跨局隔离、快速事件和结束清理测试通过。

结束摘要与清理已有自动测试覆盖，但尚未完成一次真实游戏结束回归。

### 推荐器 v3

方法标识：

- `contextual_state_v3`
- `contextual_state_plus_community_v3`

已实现因素：

- 基础伤害、格挡、抽牌、能量、范围、降费、成长和生命代价；
- 当前牌组的输出、防御、抽牌、范围和费用缺口；
- 重复牌与牌组效果协同；
- 遗物与攻击、技能、格挡、抽牌、能量等效果协同；
- 当前 HP、章节、楼层和最近三步可选路径压力；
- 升级、附魔和负面状态；
- 带来源、样本量和有限权重的社区先验。

使用当前真实检查点和三张已知候选进行离线回放：

| 候选 | 状态分 | 综合分 |
|---|---:|---:|
| 踩踏 | 62.90 | 60.26 |
| 耸肩无视 | 55.95 | 55.46 |
| 武装 | 56.50 | 54.56 |
| 跳过 | — | 31.60 |

当前地图只有一条三步可达路径，精英路径比例为 100%；每张候选只产生一个
`route_elite_fit`，不再同时叠加通用威胁和精英威胁。该结果只是规则适配分，
不是胜率。

### 结构化效果标签

- SQLite schema v7 使用 `entity_effect_tags`，当前标签版本为 v3；
- 使用稳定实体 ID 保存 `tag + magnitude + source_field`；
- 数值字段优先，版本化描述规则只补充结构化缺口；
- 标签可从 catalog 全量重建，不调用网络、RAG 或 LLM；
- 自身耗竭 92、虚无 16、虚无交互 5、耗竭交互 5、条件标签 2；
- 药水标签为 0；药水状态继续读取，但 P0 不参与选牌评分；
- v2 → v3 同 catalog 自动重建、正例、负例和评分测试通过；
- 真实运行数据库已经完成 v3 重建。

### 动态跳过和面板高亮

- 跳过不再固定为 50；
- 动态考虑牌组大小、功能缺口、候选状态分、重复度和路线压力；
- 社区先验不能单独触发跳过；
- 建议新增 `recommended_option_index`；
- Mod 高亮该最终索引，不再对“不可推荐的跳过”和卡牌显示分直接取最大值；
- 大牌组跳过分上升、强候选跳过分下降、`can_skip=false` 等测试通过。

### 固定选牌场景评测

- `data/advisor_eval_scenarios.json` 包含 26 个可人工审阅的真实 ID 场景；
- 覆盖牌组功能缺口、HP/章节、遗物、牌组协同、路线压力和跳过；
- 评测器使用临时 SQLite，不读取当前局、历史选择、本机 tier 或网络；
- 当前基线为 25/26 场景、92/93 断言通过；
- 唯一失败项是弹珠袋“开场易伤窗口收益”与“重复易伤重叠”的净协同尚未建模；
- 该结果是规则回归基线，不是胜率、训练准确率或真机完成证明。

### 游戏内 UI 工作区版本

- 左侧半透明抽屉，视口和内容自适应宽度；
- 展开/收起箭头和 0.22 秒动画；
- 显示实际候选中文名和跳过；
- 标题字号 20，候选字号 18，箭头字号 26；
- 候选数量动态布局；
- `run_id + event_id + 候选稳定 ID` 全部匹配后才显示；
- C# 编译 0 error。

更大字号和推荐索引高亮已经安装；仍需下一次真实选牌确认显示和最终推荐一致。

## 数据现状

- 结构化 catalog：576 张卡牌、288 个遗物；
- Spire Codex 卡牌统计覆盖 484/576，且是混合版本观察性数据；
- 本机 `data/local/mobalytics_card_tiers.json` 仍缺失，运行时安全降级；
- 没有奖励被提供次数，因此无法计算真实抓取率；
- P0 不保存逐次决策训练历史，因此当前不能训练或宣传校准胜率。

## 后台 EXE

- `realtime/host.py`、PyInstaller spec、构建脚本和 P0 requirements 已存在；
- 开发阶段只在真机测试时启动 host，不作为常驻服务；
- 当前 conda 环境尚未安装 PyInstaller；
- 当前 `packaging/sts2-guide.spec` 的项目根目录解析错误，构建前必须修正；
- 尚未生成和验证无控制台 EXE。

## P0 边界清理

- 已删除未引用的旧路线 fallback、历史选择权重迭代模块和重复 host 入口；
- P0 安装只依赖 `requirements-p0.txt`，不要求 API Key、Vue、FastAPI、RAG、LLM
  或网络；
- 可选 RAG/Web 实验代码仍保留，但不属于 P0 实时运行依赖；
- 本机 host 日志、评测报告、Godot 编辑器缓存、真实数据库、本机 tier 和缺少授权
  信息的攻略正文快照均不会提交；
- 清理后的完整 Python 测试为 122/122，通过数比此前少 3 个是删除旧历史迭代测试
  的预期结果。

## 当前阻塞与下一步

1. 进入下一次真实选牌，核对 v3 分数、动态跳过和推荐高亮；
2. 完成一次真实游戏结束回归，核对最终摘要和当前局清理；
3. 基于固定场景修正易伤窗口净协同，并继续人工校准路径比例、HP 和遗物权重；
4. 完成两张地图及 Boss 节点回归；
5. 完成上述回归后再打包并验收无控制台后台 EXE。

当前条件标签只覆盖易伤和中毒两个已核实模式；这不是阻塞项，不在缺少证据时继续扩张。
