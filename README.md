# STS2-Guide：杀戮尖塔2攻略助手

基于 RAG（检索增强生成）架构的游戏攻略问答系统，用户可以用自然语言提问，系统自动从知识库中检索相关内容并生成回答。

## 技术栈

- **向量模型**：sentence-transformers/all-MiniLM-L6-v2
- **向量检索**：numpy 点积相似度
- **LLM**：DeepSeek API
- **知识库**：JSON
- **语言**：Python 3.9

## 核心功能

- **语义检索**：基于向量相似度检索最相关知识，而非关键词匹配
- **自适应检索**：自动判断检索结果是否充足，不足时扩大检索范围
- **多轮对话**：保留对话历史，支持上下文关联的连续提问
- **历史截断**：自动截断过长的对话历史，避免 token 超限
- **来源透明**：每次回答显示参考来源及相似度分数
- **向量缓存**：知识库向量计算结果持久化，避免重复计算
- **错误处理**：覆盖 API 限流、认证失败、网络异常等常见错误

## 快速开始

**1. 安装依赖**
```bash
pip install openai sentence-transformers numpy python-dotenv
```

**2. 配置 API key**

**3. 运行**
```bash
python main.py
```